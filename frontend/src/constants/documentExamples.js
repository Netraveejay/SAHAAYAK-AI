/**
 * Example image URLs for document types in the Visual Aid modal.
 * Replace with your own asset URLs or keep placeholders.
 */
const BASE = 'https://placehold.co/400x260/e0f2fe/0d9488';

export const DOCUMENT_EXAMPLE_IMAGES = {
  aadhaar: `${BASE}?text=Aadhaar+Card`,
  pan: `${BASE}?text=PAN+Card`,
  income_certificate: `${BASE}?text=Income+Certificate`,
  other: `${BASE}?text=Document`,
};
